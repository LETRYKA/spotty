import express from "express";
// import { getUsername } from "../resolvers/user-profile/get-username"; example import


export const friendsRoute = express.Router();

// friendsRoute.get("/requests/:id", getUsername); Example route
